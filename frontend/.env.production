# 生产环境：与 Node 同机部署时留空，由 Express 托管 dist，接口同域 /admin-api
# 若前后端分离部署，改为实际 API 根地址
VITE_API_BASE_URL=
VITE_BUILD_DIR=dist

# 不打外链 CDN：axios/dayjs 等打进 /assets，避免访客运营商/网络拦截 npmmirror 导致白屏
VITE_USE_CDN=false